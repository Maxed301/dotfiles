require("fidget").setup({
    notification = {
        window = {
            normal_hl = "Comment",
            winblend = 0,
            align = "bottom",
            relative = "editor",
        },
    },
})
